# JobBOSS2 MCP Server environment template
# Copy to .env and fill in real values.

# Required: core API/OAuth settings used by the MCP server
JOBBOSS2_API_URL=https://your-tenant.jobboss2.example
JOBBOSS2_API_KEY=your-client-id
JOBBOSS2_API_SECRET=your-client-secret
JOBBOSS2_OAUTH_TOKEN_URL=https://your-tenant.jobboss2.example/oauth/token

# Optional: request timeout in milliseconds (default: 30000)
API_TIMEOUT=30000

# Transport configuration (optional, defaults to stdio)
# Options: stdio, http, httpStream, sse
MCP_TRANSPORT=stdio

# Port for HTTP/SSE transport (optional, defaults to 8000)
MCP_PORT=8000

# Optional: enforce read-only MCP behavior in sensitive environments.
# When set to 1/true/yes/on, mutation tools and mutating custom_api_call methods are blocked.
# JOBBOSS2_READ_ONLY_MODE=1

# Optional: enable tier-one smoke tests (staging only)
# Set to 1 only when intentionally running: bun run test:smoke:tier1
# SMOKE_TIER1=1

# Required for smoke when SMOKE_TIER1=1
# Comma-separated hostname allowlist for smoke execution.
# Example: SMOKE_ALLOWED_HOSTS=staging.example.internal
# SMOKE_ALLOWED_HOSTS=

# Optional for smoke
# Comma-separated hostname patterns to block in addition to defaults.
# Example: SMOKE_BLOCKED_HOST_PATTERNS=prod.,live.
# SMOKE_BLOCKED_HOST_PATTERNS=

# Optional for smoke
# Path to seed JSON for detail endpoint IDs.
# Example: SMOKE_TIER1_SEEDS_FILE=./tests/smoke/tier1.seeds.json
# SMOKE_TIER1_SEEDS_FILE=
